<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"> <!-- 전체 배경색 (연한 보라) -->

    <!-- 상단 타이틀 -->
    <TextView
        android:id="@+id/tv_header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="마이페이지"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 뒤로가기 아이콘 -->
    <ImageView
        android:id="@+id/btn_back"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginLeft="15dp"
        android:src="@drawable/ic_arrow_back"
        app:tint="#888888"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_header_title"
        app:layout_constraintBottom_toBottomOf="@id/tv_header_title"/>

    <!-- 프로필 영역 카드 -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_profile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:foregroundGravity="center"
        android:layout_marginTop="20dp"
        app:cardCornerRadius="20dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#F3E5F5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_header_title">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingVertical="30dp">

            <!-- 프로필 이미지 -->
            <ImageView
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:src="@drawable/ic_menu_mypage2"
                android:scaleType="fitCenter"
                app:tint="#888888"
                android:background="@drawable/bg_mypage_profile"
                android:padding="10dp"/>

            <TextView
                android:id="@+id/tv_user_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="사용자님"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="@color/black"
                android:layout_marginTop="10dp"/>

            <TextView
                android:id="@+id/tv_myPage_email"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="xxx@xxx,com"
                android:textSize="14sp"
                android:textColor="#666666"
                android:layout_marginTop="3dp"/>

            <TextView
                android:id="@+id/tv_myPage_gender"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="남"
                android:textSize="12sp"
                android:textColor="#666666"
                android:background="@drawable/bg_bottom_nav_floating"
                android:backgroundTint="@android:color/transparent"
                android:paddingHorizontal="5dp"
                android:paddingVertical="0dp"
                android:layout_marginTop="3dp"/>
            <!-- 실제로는 stroke가 있는 drawable을 쓰면 더 좋음 -->
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 탭 영역 (Big5 기반 / 키워드 기반) -->
    <LinearLayout
        android:id="@+id/layout_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="30dp"
        android:paddingHorizontal="20dp"
        app:layout_constraintTop_toBottomOf="@id/card_profile">

        <!-- 탭 1: Big5 기반 -->
        <LinearLayout
            android:id="@+id/tab_big5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginEnd="20dp">

            <TextView
                android:id="@+id/tv_tab_big5"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Big5 기반"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/colorPrimary"/>

            <View
                android:id="@+id/indicator_big5"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:background="@color/colorPrimary"
                android:layout_marginTop="5dp"/>
        </LinearLayout>

        <!-- 탭 2: 키워드 기반 -->
        <LinearLayout
            android:id="@+id/tab_keyword"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_tab_keyword"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="키워드 기반"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#AAAAAA"/>

            <View
                android:id="@+id/indicator_keyword"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:background="@color/colorPrimary"
                android:layout_marginTop="5dp"
                android:visibility="invisible"/>
        </LinearLayout>

    </LinearLayout>

    <!-- 리스트 영역 -->
    <androidx.recyclerview.widget.RecyclerView
        android:backgroundTint="@color/colorPrimary"
        android:id="@+id/rv_mypage_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="15dp"
        android:layout_marginHorizontal="15dp"
        android:clipToPadding="false"
        android:paddingBottom="20dp"
        app:layout_constraintTop_toBottomOf="@id/layout_tabs"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- 데이터 없음 표시 (필요시) -->
    <TextView
        android:id="@+id/tv_empty_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="저장된 결과가 없습니다."
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/rv_mypage_list"
        app:layout_constraintBottom_toBottomOf="@id/rv_mypage_list"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>